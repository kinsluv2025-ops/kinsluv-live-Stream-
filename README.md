Kinsluv Live Stream — Standard Edition

Requirements:
- Node.js 16+
- npm

Install & run:
1. npm install
2. Create .env (see example)
3. npm start
4. Open http://localhost:3000

Admin panel:
- Open http://localhost:3000/admin
- Use ADMIN_PASSWORD from .env (enter in admin password field)

Notes:
- Replace the <video> source in public/index.html with your HLS/MP4 live URL.
- Secure JWT_SECRET and ADMIN_PASSWORD in production.
